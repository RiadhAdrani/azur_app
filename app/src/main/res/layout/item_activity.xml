<?xml version="1.0" encoding="utf-8"?>
<androidx.cardview.widget.CardView xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="wrap_content"
    android:layout_height="wrap_content"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_margin="@dimen/epsilon"
    app:cardBackgroundColor="@color/azur_dark"
    app:cardCornerRadius="4dp"
    android:background="@drawable/pic_inside"
    android:foreground="?android:attr/selectableItemBackground">

        <TextView
            android:id="@+id/item_text"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="@string/azur_city"
            android:textColor="@color/white"
            android:textStyle="bold"
            android:textSize="@dimen/text_tiny"
            android:paddingHorizontal="@dimen/small_extra"
            android:paddingVertical="@dimen/tiny_extra"
            android:textAlignment="center"
            />

</androidx.cardview.widget.CardView>